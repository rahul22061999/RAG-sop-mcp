name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      PG_HOST: ci-placeholder
      PG_DATABASE: ci-placeholder
      PG_USER: ci-placeholder
      PG_PASSWORD: ci-placeholder
      OPENAI_API_KEY: sk-ci-placeholder
      OPENAI_EMBEDDING_MODEL: text-embedding-3-small
      UNKEY_ROOT_API_KEY: ci-placeholder
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --frozen

      - name: Lint (ruff)
        run: uv run ruff check server/ tests/

      - name: Run tests
        run: |
          set +e
          uv run pytest
          code=$?
          set -e
          # pytest exits 5 when zero tests are collected - treat that as a
          # pass rather than a failure while test coverage is still being
          # built out, instead of a false-red CI on every push.
          if [ "$code" -eq 5 ]; then
            echo "::warning::No tests collected yet."
            exit 0
          fi
          exit "$code"
